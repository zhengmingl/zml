<?php  
	//定义tree函数
	// function tree($list, $pid=0, $level=0) {
	// 	static $data = array();
	// 	foreach($list as $row) {
	// 		if($row['pid'] == $pid) {
	// 			$row['level'] = $level;
	// 			$data[] = $row;
	// 			tree($list, $row['id'], $level+1);
	// 		}
	// 	}
	// 	return $data;
	// }
?>